{
  "name": "jev-playground",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "npm run start:utterance",
    "start:utterance": "node --env-file=.env apps/utterance-playground/server.mjs",
    "test": "node --test"
  },
  "engines": {
    "node": ">=22"
  }
}
